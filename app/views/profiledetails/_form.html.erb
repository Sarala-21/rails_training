<%= form_with(model: profiledetail, local: true) do |form| %>
  <% if profiledetail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profiledetail.errors.count, "error") %> prohibited this profiledetail from being saved:</h2>

      <ul>
        <% profiledetail.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name %>
  </div>

  <div class="field">
    <%= form.label :education %>
    <%= form.text_field :education %>
  </div>

  <div class="field">
    <%= form.label :experience %>
    <%= form.text_field :experience %>
  </div>

  <div class="field">
    <%= form.number_field :user_id, id: :profiledetail_user_id, class: "form-control", value: current_user.id, type: :hidden  %>
  </div>
  
  <div class="field">
    <%= form.label :resume %>
    <%= form.file_field :resume, accept: 'image/png, image/jpg' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
